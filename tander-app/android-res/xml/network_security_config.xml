<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow all domains in debug/development mode -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <!-- Trust system certificates -->
            <certificates src="system" />
            <!-- Trust user-added certificates (for development) -->
            <certificates src="user" />
        </trust-anchors>
    </base-config>

    <!-- Specific configuration for Cloudflare Tunnel domains -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">trycloudflare.com</domain>
        <domain includeSubdomains="true">ngrok.io</domain>
        <domain includeSubdomains="true">ngrok.app</domain>
        <domain includeSubdomains="true">localhost</domain>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
</network-security-config>
